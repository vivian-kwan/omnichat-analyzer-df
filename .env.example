PORT=3000
EXTENSION_SECRET=change-this-to-a-random-string
ADMIN_PASSWORD=change-this-admin-password

# Local dev only — never set this on a deployed server. Enables POST /api/dev/sync-copy,
# which shells out to docs/sync_copy_from_xlsx.py (reads a local Google Drive path).
ENABLE_DEV_TOOLS=false
